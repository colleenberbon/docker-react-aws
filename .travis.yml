sudo: required
language: generic

services:
  - docker

before install:
  - docker build -t colleenberbon/docker-react-travis -f Dockerfile.dev .

script:
  - docker run -e CI=true colleenberbon/docker-react-travis npm run test 